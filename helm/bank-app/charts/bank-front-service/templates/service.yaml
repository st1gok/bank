apiVersion: v1
kind: Service
metadata:
  name: {{ include "bank-front-service.fullname" . }}
  labels:
    {{- include "bank-front-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bank-front-service.selectorLabels" . | nindent 4 }}
